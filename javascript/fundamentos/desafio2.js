// pi * raio * raio 

const raio = 5; 
const pi = 3.14;
let area = pi * raio * raio;

console.log( " O valor da circunferencia é "+area+ "m2");

area = Math.PI * raio * raio;
console.log( " O valor da circunferencia é "+area+ "m2");